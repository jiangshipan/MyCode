package offer2;/** * 调整数组顺序使奇数位于偶数前面 */public class TestThree {    public static void main(String[] args) {        int[] array = new int[]{1,3,2,4,6,7,8,3,5};        reOrderArray(array);    }    public static void reOrderArray(int [] array) {        int length = 0;        for (int i = 0; i < array.length; i++) {            if (array[i] % 2 == 0) {                length++;            }        }        //存放偶数的位置        int[] pos = new int[length];        //获取pos元素        int j = 0;        //存储pos元素        int k = 0;        for (int i = 0; i < array.length; i++) {            if (array[i] % 2 == 0) {                pos[k++] = i;            }            if (array[i] % 2 == 1 && k != 0){                int temp = array[i];                //i 和 pos[j] 之间的元素向后移动一位                for (int a = i - 1; a > pos[j]; a--) {                    int temp2 = array[a - 1];                    array[a + 1] = array[a];                    array[a] = temp2;                }                //pos里的位置 + 1                for (int b = 0; b < k; b++) {                    pos[b] = pos[b] + 1;                }                array[pos[j] - 1] = temp;                j++;            }        }        for (int i : array) {            System.out.println(array[i]);        }    }}