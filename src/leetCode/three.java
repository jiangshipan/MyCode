package leetCode;import java.util.*;public class three {        public static void main(String[] args) {            Scanner sc = new Scanner(System.in);//            int[] A = new int[4];//            for (int i = 0; i < A.length; i++) {//                A[i] = sc.nextInt();//            }            int[] A = new int[]{3,4,1,2};            Sort(A);            System.out.println(Arrays.toString(A));            //System.out.println(getMax(A));        }        public static int getMax(int[] A) {            Sort(A);            int sum = A[A.length - 1] + A[A.length - 2] + A[A.length - 3];            return sum;        }        public static void Sort(int[] a) {            for (int i = 0; i < a.length - 1; i++) {                boolean isSorted = true;                int border = a.length - 1;                int newBorder = 0;                for (int j = 0; j < border; j++) {                    if (a[j] < 0 || a[j + 1] < 0) {                        a[j] = Math.abs(a[j]);                        a[j + 1] = Math.abs(a[j+1]);                    }                    if (a[j] > a[j + 1]) {                        int temp = a[j];                        a[j] = a[j + 1];                        a[j + 1] = temp;                        isSorted = false;                    }                    newBorder = j;                }                border = newBorder;                if (isSorted) {                    break;                }            }        }}